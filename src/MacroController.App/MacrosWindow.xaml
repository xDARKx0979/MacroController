<Window x:Class="MacroController.App.MacrosWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Macros" Height="440" Width="560"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Macros" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <ListBox x:Name="MacroList" Grid.Row="1" Margin="0,0,0,10">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="2,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Subtitle}" Foreground="Gray" FontSize="11"/>
                        </StackPanel>

                        <Button Grid.Column="1" Content="Edit" Tag="{Binding}" Click="EditButton_Click" Padding="10,4" Margin="6,0"/>
                        <Button Grid.Column="2" Content="Duplicate" Tag="{Binding}" Click="DuplicateButton_Click" Padding="10,4" Margin="0,0,6,0"/>
                        <Button Grid.Column="3" Content="Export..." Tag="{Binding}" Click="ExportButton_Click" Padding="10,4" Margin="0,0,6,0"/>
                        <Button Grid.Column="4" Content="Delete" Tag="{Binding}" Click="DeleteButton_Click" Padding="10,4"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Import..." Click="ImportButton_Click" Padding="10,4" Margin="0,0,8,0"/>
            <Button Content="Make Macro..." Click="NewMacroButton_Click" Padding="10,4" Margin="0,0,8,0"/>
            <Button Content="Close" IsCancel="True" Padding="10,4"/>
        </StackPanel>
    </Grid>
</Window>
