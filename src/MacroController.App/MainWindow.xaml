<Window x:Class="MacroController.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MacroController.App"
        mc:Ignorable="d"
        Title="MacroController" Height="440" Width="560"
        Icon="Assets/icon.ico">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Macros" FontSize="22" FontWeight="Bold" Margin="0,0,0,14"/>

        <ListBox x:Name="MacroList" Grid.Row="1" Margin="0,0,0,14"
                 Background="Transparent" BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,0,0,8"/>
                    <Setter Property="Focusable" Value="False"/>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="{StaticResource CardBrush}" CornerRadius="8" Padding="14,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14"/>
                                <TextBlock Text="{Binding Subtitle}" Foreground="{StaticResource SubtleTextBrush}" FontSize="11" Margin="0,2,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                <Button Content="Edit" Tag="{Binding}" Click="EditButton_Click" Style="{StaticResource ListRowButton}" Margin="0"/>
                                <Button Content="Duplicate" Tag="{Binding}" Click="DuplicateButton_Click" Style="{StaticResource ListRowButton}"/>
                                <Button Content="Export" Tag="{Binding}" Click="ExportButton_Click" Style="{StaticResource ListRowButton}"/>
                                <Button Content="Delete" Tag="{Binding}" Click="DeleteButton_Click" Style="{StaticResource ListRowButton}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Import" Click="ImportButton_Click" Padding="10,4" Margin="0,0,8,0"/>
            <Button Content="Make Macro" Click="NewMacroButton_Click" Style="{StaticResource PrimaryButton}"/>
        </StackPanel>
    </Grid>
</Window>
