<Window x:Class="MacroController.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MacroController.App"
        mc:Ignorable="d"
        Title="MacroController" Height="220" Width="400" ResizeMode="CanMinimize"
        Icon="Assets/icon.ico">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,6">
            <TextBlock Text="Active profile:" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock x:Name="ActiveProfileText" Text="(none)" Margin="6,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <CheckBox Grid.Row="1" x:Name="StartWithWindowsCheckBox" Content="Start with Windows"
                  Margin="0,0,0,16" Checked="StartWithWindowsCheckBox_Changed" Unchecked="StartWithWindowsCheckBox_Changed"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Stretch">
            <Button x:Name="MacrosButton" Content="Macros" Click="MacrosButton_Click"
                    FontSize="16" Width="175" Height="60" Margin="0,0,8,0"/>
            <Button x:Name="ProfilesButton" Content="Profiles" Click="ProfilesButton_Click"
                    FontSize="16" Width="175" Height="60"/>
        </StackPanel>
    </Grid>
</Window>
