<Window x:Class="MacroController.App.UpdateProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MacroController"
        Height="120" Width="340"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" Topmost="True"
        ShowInTaskbar="False"
        Icon="Assets/icon.ico"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border Background="#181A20" CornerRadius="10">
        <Grid Margin="20,16,20,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="MacroController"
                       Foreground="#7C5CFC" FontWeight="Bold" FontSize="13"/>

            <TextBlock x:Name="StatusText" Grid.Row="1" Text="Downloading update..."
                       Foreground="#C8C8D2" FontSize="12" Margin="0,12,0,8"/>

            <ProgressBar x:Name="ProgressBarControl" Grid.Row="2" Height="6"
                         Minimum="0" Maximum="100"
                         Background="#2A2D36" Foreground="#4F8EF7" BorderThickness="0"/>

            <Border Grid.Row="3" Background="#4F8EF7" Height="3"
                    HorizontalAlignment="Stretch" VerticalAlignment="Bottom"
                    Margin="-20,0,-20,0"/>
        </Grid>
    </Border>
</Window>
