<Window x:Class="MacroController.App.ProfileEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Edit Profile" Height="520" Width="540"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Name:" Width="110" VerticalAlignment="Center"/>
            <TextBox x:Name="ProfileNameInput" Width="300"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,16">
            <CheckBox x:Name="AutoApplyCheckBox" Content="Automatically apply this profile when a matching window is focused"
                      Checked="AutoApplyCheckBox_Changed" Unchecked="AutoApplyCheckBox_Changed"/>
            <StackPanel Orientation="Horizontal" Margin="22,6,0,0">
                <TextBlock Text="Window name:" Width="110" VerticalAlignment="Center"/>
                <TextBox x:Name="WindowNameInput" Width="300"
                         ToolTip="Process name without .exe (e.g. notepad). Multiple names can be comma-separated."/>
            </StackPanel>
        </StackPanel>

        <TextBlock Grid.Row="2" Text="Bindings" FontWeight="Bold" Margin="0,0,0,4"/>

        <ListBox x:Name="BindingList" Grid.Row="3" MouseDoubleClick="BindingList_MouseDoubleClick" Margin="0,0,0,6"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,0,0,16">
            <Button Content="Add Binding..." Click="AddBinding_Click" Padding="6,2" Margin="0,0,4,0"/>
            <Button Content="Edit Binding..." Click="EditBinding_Click" Padding="6,2" Margin="0,0,4,0"/>
            <Button Content="Remove Binding" Click="RemoveBinding_Click" Padding="6,2"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" IsDefault="True" Click="SaveButton_Click" Width="80" Margin="0,0,8,0"/>
            <Button Content="Cancel" IsCancel="True" Width="80"/>
        </StackPanel>
    </Grid>
</Window>
